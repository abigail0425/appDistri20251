-- En la carpeta donde esta el archivo yml abrir la consola
 cmd como ADMINISTRADOR y digitar el siguiente comando para instalar kong con docker:

docker-compose -f .\docker-compose-gateway.yml up -d



